# Build Progress

Tracks progress through [Build_Plan.md](Build_Plan.md). Tick tasks as they're completed. Add notes under a task for decisions, deviations, or follow-ups worth remembering.

**Legend:** `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked (needs user action)

---

## Phase 1: Foundation & Property Page

- [x] **Task 1.1** — Project scaffolding (Next.js, TS strict, Tailwind, shadcn/ui, structure, lint)
- [x] **Task 1.2** — Supabase setup & Prisma configuration _(local Supabase via CLI; hosted instance deferred to deploy time)_
- [x] **Task 1.3** — Supabase Storage setup
- [x] **Task 1.4** — Property listing page — photo gallery
- [x] **Task 1.5** — Property listing page — description & amenities
- [x] **Task 1.6** — Property listing page — location map
- [x] **Task 1.7** — SEO & metadata
- [x] **Task 1.8** — Vercel deployment

**🎉 Phase 1 complete — site live at https://staithes.vercel.app**

## Phase 2: Calendar & Booking Engine (MVP)

- [x] **Task 2.1** — Database schema — booking entities
- [x] **Task 2.2** — Availability checking
- [x] **Task 2.3** — Pricing engine (Phase 2 simple version; full rules engine deferred to Phase 7)
- [x] **Task 2.4** — Availability calendar component
- [x] **Task 2.5** — Supabase Auth — guest authentication
- [x] **Task 2.6** — Stripe integration — checkout session
- [x] **Task 2.7** — Stripe webhook handling
- [x] **Task 2.8** — Email — booking confirmation
- [x] **Task 2.9** — Booking flow UI
- [x] **Task 2.10** — Booking expiry cron job

**🎯 MVP launch point reached — Phase 2 complete**

## Phase 3: Guest & Host Dashboards

- [x] **Task 3.1** — Guest dashboard
- [x] **Task 3.2** — Booking detail page (guest)
- [x] **Task 3.3** — Host admin panel — layout & auth
- [x] **Task 3.4** — Host admin — booking management
- [x] **Task 3.5** — Host admin — calendar view
- [x] **Task 3.6** — Host admin — dashboard overview

**🎉 Phase 3 complete — guest and host dashboards both live**

## Phase 4: Communication

- [ ] **Task 4.1** — Database schema — messaging entities
- [ ] **Task 4.2** — Messaging API
- [ ] **Task 4.3** — Messaging UI — guest side
- [ ] **Task 4.4** — Messaging UI — admin side
- [ ] **Task 4.5** — Supabase Realtime — live messaging

## Phase 5: Automated Email Sequences

- [ ] **Task 5.1** — Email templates
- [ ] **Task 5.2** — Email scheduler
- [ ] **Task 5.3** — Daily host summary

## Phase 6: Reviews & Social Proof

- [ ] **Task 6.1** — Database schema — reviews
- [ ] **Task 6.2** — Review submission
- [ ] **Task 6.3** — Review display on property page
- [ ] **Task 6.4** — Host review management

## Phase 7: Advanced Pricing & Financial Admin

- [ ] **Task 7.1** — Pricing rules management UI
- [ ] **Task 7.2** — Pricing engine — full implementation
- [ ] **Task 7.3** — Financial reports

## Phase 8: Polish & Extended Features

- [ ] **Task 8.1** — Cancellation flow
- [ ] **Task 8.2** — Request-to-book mode
- [ ] **Task 8.3** — Property content management
- [ ] **Task 8.4** — Site settings management
- [ ] **Task 8.5** — SEO refinements

## Phase 9: Growth Features

- [ ] **Task 9.1** — Booking analytics
- [ ] **Task 9.2** — Website analytics integration
- [ ] **Task 9.3** — Email marketing — guest list & newsletters
- [ ] **Task 9.4** — Multi-language support (optional)

---

## Notes & decisions

_Record non-obvious choices, deviations from the plan, and things a future session would need to know. Keep it tight._

- **Task 1.1**: Scaffolded with `create-next-app` (Next 16, React 19, Tailwind v4, Turbopack, App Router, no `src/` dir, `@/*` import alias). shadcn/ui initialised with the `neutral` base via `--defaults`. Home page lives at `app/(guest)/page.tsx`; root layout in `app/layout.tsx` wraps all routes with `Header`/`Footer` from `components/layout/`. Prettier configured with `prettier-plugin-tailwindcss`. `.env.example` committed; `.env.local` gitignored.
- **Task 3.6**: Host dashboard overview at [/admin/dashboard](app/admin/dashboard/page.tsx) — five summary cards (today's arrivals, today's check-outs, next-30-day count + revenue, this-month's revenue, this-month's occupancy) plus two list columns (next 5 upcoming, recent 5 created). All `Promise.all`'d so it's a single round-trip. Revenue uses `Prisma.Decimal` arithmetic end-to-end. **Occupancy is computed as confirmed-booking nights / days-in-month** — booking dates are clamped to the month window via a `nightsInMonth` helper. **"This month's revenue" means stays starting this month**, not money received this month — the host cares about which stays they're earning from, not when the cash hit Stripe. The placeholder dashboard is gone; clicking around the admin should now feel like a small but real product.
- **Task 3.5**: [/admin/calendar](app/admin/calendar/page.tsx) — month view of the property with bookings + blocked dates coloured. Server fetches bookings (pending/confirmed only) and blocked dates for a window slightly larger than the visible month so spillover days at grid edges are also coloured. Builds a flat day-keyed `Record<string, CalendarDayEntry>` with bookings overlaying blocked dates so clicking always lands on a booking when both overlap. [AdminCalendar](components/admin/admin-calendar.tsx) is the client component on react-day-picker (`mode="single"`, `hideNavigation` because we have our own prev/next that drive a `?month=YYYY-MM` query on the server). **Two-click range picker for blocking**: first click on an empty day shows a sky-blue hint banner above the calendar and rings the picked day; second click opens a confirm-only modal pre-filled with the range and a reason input. Escape cancels the pick. Clicking a booking jumps to `/admin/bookings/[id]`; clicking a blocked range opens a separate view-blocked modal with an Unblock button. Two new endpoints under [/api/admin/blocked-dates](app/api/admin/blocked-dates/route.ts) and `[id]/route.ts` — POST and DELETE — both with **belt-and-braces auth** (a `requireHost()` helper that runs even though the proxy already gates `/admin/*`). **Bug fix worth remembering**: the modal `submitting` state must be reset in `closeModal`, otherwise after the first successful submit the buttons stay disabled with "Unblocking…" text on the next open (the client component persists across `router.refresh()`).
- **Task 3.4**: Host booking management. [/admin/bookings](app/admin/bookings/page.tsx) is a server-rendered table with **filters as plain links** (no JS, no client state) — Status chips (all/pending/confirmed/completed/cancelled/no_show), When chips (upcoming/past/all time), and a `<form method="get">` for the search input. Search matches first/last name and email with `contains` + `mode: insensitive`. Default view is upcoming bookings only — limits noise for the host. The `buildUrl` helper is intentionally tiny: omits default values from the query string so URLs stay short. Table caps at 200 rows for now — pagination's a Phase 8 nice-to-have. [/admin/bookings/[id]](app/admin/bookings/[id]/page.tsx) is the host's view of a single booking. **Always shows the full address and full guest contact** — no privacy gating like the guest detail page does. Email is a clickable `mailto:`. Payments panel includes the Stripe transaction id for cross-referencing. Refund and host-cancel actions are deliberately stubbed with a "Phase 8" footnote — the cancellation flow is its own task. The admin nav now has a Bookings link.
- **Task 3.3**: Host role via the simplest possible mechanism — `HOST_EMAILS` env var (comma-separated). [`isHostEmail`](lib/auth/host.ts) compares case-insensitively. **Three layers of gating** so no single failure exposes admin content: (1) [proxy.ts](proxy.ts)'s `updateSession` redirects unauthed users to `/login?next=/admin/...` and signed-in non-hosts to `/dashboard`; (2) [app/admin/layout.tsx](app/admin/layout.tsx) re-checks both conditions in the server component before rendering, so a missing proxy rule or a future regression doesn't expose data; (3) the proxy also overrides the `signed-in user on /login → /dashboard` rule for hosts so they land on `/admin/dashboard` instead. Sign-in action's fallback redirect honours the host role too. **File-structure fix**: the `(admin)/dashboard` route group from Task 1.1's scaffolding would have collided with `(guest)/dashboard` because route groups don't add to the URL — both resolved to `/dashboard`. Restructured: admin pages now live at real `/admin/*` URLs (no route group), guest chrome moved into the `(guest)` group via [app/(guest)/layout.tsx](app/(guest)/layout.tsx), root layout stripped down to just html/body/fonts so admin can have its own dark chrome via [app/admin/layout.tsx](app/admin/layout.tsx). Admin nav in [components/admin/admin-header.tsx](components/admin/admin-header.tsx) only shows links to routes that exist — added per task as we build them. Today: just Dashboard.
- **Tasks 3.1 + 3.2**: Guest dashboard at [app/(guest)/dashboard/page.tsx](app/(guest)/dashboard/page.tsx) — server component, gates auth explicitly (proxy already does this but the explicit check keeps the page safe in isolation and narrows the type for the guest fetch). Bookings are sectioned by status: in-progress, upcoming, awaiting payment, past stays, cancelled — with empty section hiding so the layout doesn't show empty headers. Empty state for users with zero bookings has a "Browse the property" CTA. Booking detail page at [app/(guest)/dashboard/bookings/[id]/page.tsx](app/(guest)/dashboard/bookings/[id]/page.tsx) — **404s on both "doesn't exist" and "exists but not yours"**, deliberately not leaking ownership. Shows the hero photo, dates with check-in/out times, guest count, the optional message-to-host, and a sidebar with the full price snapshot + payment row (card brand + last4 from the Stripe expand call in the webhook). **Address reveal pattern**: full address only shown when status is `confirmed` or `completed`, otherwise the approximate address with a "exact address shared once confirmed" note (per the data model spec). Shared [BookingStatusBadge](components/booking/status-badge.tsx) and [BookingCard](components/booking/booking-card.tsx) so the host admin in 3.4 can reuse them.
- **Task 2.10**: [`expireStalePendingBookings`](lib/booking/expire.ts) is the safety-net sweep that cancels `pending` bookings older than `PENDING_TTL_MINUTES` (30 — same constant as `lib/booking/create.ts`). The Stripe webhook normally beats this to it via `checkout.session.expired`, but the cron exists for cases where the webhook never fires. [`/api/cron/expire-bookings`](app/api/cron/expire-bookings/route.ts) accepts both GET and POST, bearer-auth via `CRON_SECRET` (Vercel Cron sends it automatically when the env var is set). [`vercel.json`](vercel.json) declares a `*/10 * * * *` schedule — well within the hobby tier limit. **Smoke test in [scripts/test-expire-bookings.ts](scripts/test-expire-bookings.ts)** creates a synthetic 31-minute-old pending booking + a fresh one, runs the sweep, asserts the stale one is cancelled with the right reason and the fresh one is untouched, and verifies the second sweep is a no-op (idempotent). All 6 assertions pass. Live route handler verified locally with curl: 401 with no auth, 401 with wrong token, 200 with correct bearer. **`CRON_SECRET`** generated locally with `openssl rand -hex 32`, in `.env.local` and `.env.production` and Vercel (Production scope only).
- **Task 2.9**: Dedicated `/book` review page at [app/(guest)/book/page.tsx](app/(guest)/book/page.tsx) — server component that re-parses query params with Zod, redirects unauthed visitors to `/login?next=/book?...`, then re-runs availability + price calculation server-side before rendering. Never trusts the URL params for either availability or pricing — the client could be stale or malicious. Calendar's "Reserve" button now `router.push`es to `/book?...` instead of POSTing directly to `/api/bookings` — the booking-creation POST happens from the review page's [BookingReviewForm](components/booking/review-form.tsx) (client) after the user adds an optional message. Auth flow now honours a `next` param: signin/signup forms have a hidden input, server actions read it via [`safeNext`](lib/auth/safe-redirect.ts) (rejects anything that isn't a same-origin relative path, guarding against open-redirect), and the post-login redirect lands the user back on the exact review page they came from. **Two React/Next gotchas worth remembering**: (a) the `children` prop name conflicts with React's special children handling — passing `children={0}` triggers `react/no-children-prop` lint and confuses the JSX type system; renamed to `numChildren` everywhere it crosses a component boundary. (b) Classic React zero-leak: `parsed.data.children && parsed.data.children > 0 && (...)` renders the literal `0` when children is `0` because `0 && anything` short-circuits to `0`, not `false` — use `(value ?? 0) > 0` instead.
- **Task 2.8**: Real booking confirmation email via `resend` 6.10 + `@react-email/components` 1.0.12 + `@react-email/render` 2.0.6. [lib/email/client.ts](lib/email/client.ts) — lazy Resend singleton + `senderEmail()` helper that throws on missing `RESEND_FROM_EMAIL`. [lib/email/templates/booking-confirmation.tsx](lib/email/templates/booking-confirmation.tsx) — React Email component with the property name, dates, nights, and total in a styled card. The webhook handler from 2.7 already calls `sendBookingConfirmationEmail` so wiring it up was a single-file swap of the stub. **`@react-email/render` must be installed explicitly** alongside `@react-email/components` — `resend` 6.x imports it directly, and `components` no longer transitively pulls it in (the SDK throws "Failed to render React component" without it). **Email failures are caught and logged, not rethrown** — the booking is already confirmed in the DB at the point we send and we don't want a flaky email provider to trigger Stripe webhook retries which would risk double-confirming. **Resend free dev sender caveat**: `onboarding@resend.dev` only delivers to the email address that owns the Resend account — fine for the host's own testing but useless for guest signups under different addresses. Verifying a custom sender domain is a launch-time task. **Host notification email is intentionally deferred to Task 5.3 (daily host summary)** — sending an immediate per-booking email isn't worth the extra failure mode and sender-domain question when there's only one host and the dashboard surfaces new bookings already.
- **Task 2.7**: Webhook handler at [app/api/webhooks/stripe/route.ts](app/api/webhooks/stripe/route.ts). Verifies the signature against `STRIPE_WEBHOOK_SECRET` using the **raw request body** (`request.text()` — Next.js JSON parsing would mutate it). Idempotency via a new [`WebhookEvent`](prisma/schema.prisma) table keyed on the Stripe event id: insert-or-fail-200 means duplicate deliveries are no-ops. On `checkout.session.completed` the handler calls [`confirmBooking`](lib/booking/confirm.ts) which atomically marks the Booking `confirmed` + writes a `Payment` row with the charge details (card brand/last4 are pulled by expanding `latest_charge.payment_method_details.card` on the PaymentIntent). On `checkout.session.expired` it calls `cancelPendingBooking` (also reachable from the 2.10 cron sweep). Booking confirmation email is currently a stub at [lib/email/booking.ts](lib/email/booking.ts) that just logs — Task 2.8 swaps in Resend + React Email. **If the handler throws after inserting the WebhookEvent row, the row is rolled back** so Stripe's retry will re-process the event rather than skip it. **Local testing requires** `stripe listen --forward-to http://localhost:3000/api/webhooks/stripe` running alongside `npm run dev`, with the printed `whsec_...` value pasted into `.env.local` as `STRIPE_WEBHOOK_SECRET`. Verified end-to-end: pay with test card → webhook fires → booking moves pending→confirmed → payment row written → email stub logged.
- **Task 2.6**: Stripe Checkout integration via `stripe` 22.0.1. [lib/stripe/client.ts](lib/stripe/client.ts) — lazy server-side singleton. [lib/booking/create.ts](lib/booking/create.ts) — `createBooking` orchestrates the full happy path: inside `withPropertyLock` it re-validates availability, calculates the price, and atomically inserts a `Booking` (status `pending`) plus its `BookingPriceSnapshot`; AFTER releasing the lock it creates a Stripe Checkout Session with multi-line items (accommodation, cleaning, extra guests) and embeds `booking_id` in metadata so the webhook in 2.7 can find it. Stripe rejection rolls back the pending booking immediately rather than waiting for the cron sweep. **Pending bookings hold dates for 30 minutes** (`PENDING_TTL_MINUTES`) — same as the Stripe session `expires_at` and the cron sweep cadence in 2.10. `POST /api/bookings` — auth-gated, Zod-validated, returns `{ bookingId, checkoutUrl }`. `/booking/success` and `/booking/cancel` are simple landing pages; the success page deliberately does NOT mark the booking confirmed (the webhook does that, otherwise a malicious user could fake a confirmation by hand-crafting the URL). Calendar wired up: `isSignedIn` prop drives "Reserve" vs "Sign in to book". On Reserve, the client POSTs to the endpoint and `window.location.href = url` to Stripe. **Two Prisma 7 Json gotchas**: (a) `nightlyRates: NightlyRate[]` and (b) the cancellation policy snapshot literal both fail the `InputJsonValue` index-signature check — cast via `as Prisma.InputJsonValue` (or `as unknown as` for the array case). **Calendar UX iteration**: future taken dates now visually distinct from past dates — strikethrough grey for "blocked or someone else's booking", emerald-100 highlight for "your own pending/confirmed booking" (the API returns `{ blocked, mine }` separately when the user is signed in). The user's own booked dates are still disabled — no double-booking yourself.
- **Task 2.5**: Supabase Auth via `@supabase/ssr` 0.10.2. Helpers in [lib/auth/](lib/auth/): `server.ts` (server component / route handler client + `getCurrentUser()`), `browser.ts` (singleton browser client), `middleware.ts` (`updateSession` runs `auth.getUser()` to refresh tokens, then gates `/dashboard|/messages|/admin` for unauthed users and bounces signed-in users away from `/login|/signup`). **Next 16 rename**: the file convention is `proxy.ts` not `middleware.ts` — Next deprecated the name to disambiguate from Express middleware. Function export is `proxy`, not `middleware`. Server actions in [lib/auth/actions.ts](lib/auth/actions.ts): `signUpAction`, `signInAction`, `magicLinkAction`, `signOutAction`. **Signup creates a `Guest` row in the same transaction as the auth user**, using the auth user's UUID as the Guest id (the schema's intended pattern from 2.1). Email-confirmation behaviour is handled by branching on `data.session` — if confirmation is on, signUp returns no session and the form shows a "check your email" message; if off, it redirects straight to `/dashboard`. Sign-in form has both password and magic-link options. `/auth/callback` route exchanges the magic-link / confirmation code for a session. Header is now an async server component that shows "Hi, {firstName}" and a sign-out form when authed. **Side effect of the proxy**: every page is now `ƒ` dynamic (was `○` static) because `auth.getUser()` runs per-request — fine for now, the home page renders fast. **Local Supabase has email confirmation off** for dev convenience (per [supabase/config.toml](supabase/config.toml#L208)); the **hosted** project is currently in the same state for testing but the user has chosen to flip it back to required-confirmation before launch (set `Authentication → Email → Confirm email` in the Supabase dashboard). **Open follow-up**: the dashboard page itself (Phase 3) doesn't exist yet, so signing up redirects to a 404. The auth flow still works correctly — proxy authorisation, session refresh, sign-in/out are all wired.
- **Task 2.4**: [AvailabilityCalendar](components/property/availability-calendar.tsx) — client component built on shadcn's `Calendar` (react-day-picker v9 in `mode="range"`). Fetches `/api/availability/blocked` once on mount and turns the day list into a `Set` for O(1) disabled-day matcher. On range change, calls `/api/pricing` for a live breakdown — no debounce yet because the calculator is fast. Adults/Children dropdowns with totals capped at `maxGuests`; min-stay/max-stay enforced client-side too so the user gets immediate feedback before they click Reserve. **Two shadcn-default visual fixes worth remembering**: (a) the default `today` style uses `bg-muted` which is the same fill as `range_middle`/`range_end` — overrode to a subtle ring so today doesn't visually merge with selected ranges; (b) Tailwind v4 dropped the legacy `cursor: pointer` button default — restored on enabled day cells via `[&>button]:cursor-pointer`. Subtle gotcha: putting the cursor on the cell rather than the inner button gives disabled cells the pointer too, because shadcn `Button` has `disabled:pointer-events-none` and the cursor falls through to the parent. Reserve button is wired but non-functional — the booking flow lands in 2.6/2.9.
- **Task 2.3**: [`calculatePrice`](lib/pricing/calculate.ts) returns a `PriceBreakdown` shaped to match `BookingPriceSnapshot` 1:1, so the same object can be passed straight to the booking-creation step in 2.6/2.9 with no remapping. **Phase 2 is intentionally simple**: flat `baseNightlyRate` per night, cleaning fee, extra-guest fee for guests above `baseGuestCount`, no discounts/service fee/tax. The full rules engine (seasonal/day-of-week/last-minute/early-bird/length discounts) lands in Phase 7 — when it does, only the per-night rate-resolution loop changes; the breakdown shape stays the same. **Money is `Prisma.Decimal` end-to-end**: serialised to fixed-2 decimal strings at the JSON boundary to avoid float drift. Validation split: pricing rejects only `invalid_range`/`property_not_found`/`too_many_guests` — `min_stay`/`max_stay` are availability's job, so the calculator can be called even for short preview windows. `GET /api/pricing?checkIn&checkOut&adults&children` returns the breakdown. 16/16 smoke tests in [scripts/test-pricing.ts](scripts/test-pricing.ts).
- **Task 2.2**: Availability service in [lib/availability/](lib/availability/) — `checkAvailability` returns a discriminated union of `{available, numNights}` or `{available:false, reason, message}` with reasons `in_past`, `invalid_range`, `min_stay`, `max_stay`, `blocked`, `booked`. **Date semantics nailed down**: bookings use half-open `[checkIn, checkOut)` (back-to-back stays allowed); blocked dates use closed `[dateStart, dateEnd]`. Date helpers in [dates.ts](lib/availability/dates.ts) keep everything in UTC. [`withPropertyLock`](lib/availability/lock.ts) wraps a callback in a Postgres `pg_advisory_xact_lock(hashtextextended('property:<id>', 0))` so concurrent booking attempts on the same property serialise — used by booking creation, not by read-only checks. Two new endpoints: `GET /api/availability?checkIn&checkOut` and `GET /api/availability/blocked?from&to` (defaults to today + 365 days, returns flat day-string array combining blocked + active bookings). Zod-validated query params. **Lazy Prisma singleton**: refactored [lib/db/prisma.ts](lib/db/prisma.ts) to use a Proxy so the underlying client is constructed on first access, not at module load — fixes CLI scripts whose `import` statements get hoisted above their `dotenv` calls (was crashing `scripts/test-availability.ts` with "DATABASE_URL is not set"). Hand-rolled smoke test in [scripts/test-availability.ts](scripts/test-availability.ts) — 13 assertions covering happy path, range validation, min/max stay, blocked-date overlap with half-open semantics, booking overlap, back-to-back booking allowed, and lock helper execution. Re-runnable, self-cleaning. No test framework yet — revisit when assertion count grows.
- **Task 2.1**: Six new entities in [prisma/schema.prisma](prisma/schema.prisma) — `Guest`, `Booking`, `BookingPriceSnapshot`, `Payment`, `BlockedDate`, `PricingRule` — plus enums (`BookingStatus`, `BookingType`, `PaymentType`, `PaymentStatus`, `PricingRuleType`). Field definitions follow the data-model doc. Snapshot principle is enforced by storing `cancellation_policy_snapshot` (Json) on `Booking` and the full itemised price as a one-to-one `BookingPriceSnapshot`. **`Guest.id` has no `@default(uuid())`** — it's intended to mirror `auth.users.id` from Supabase Auth so a single UUID identifies a user across both schemas; the row is created at signup time using the auth user id. No `password_hash` field — credentials live entirely in Supabase Auth. Indexes added: `(propertyId, checkIn, checkOut, status)` on bookings (the hot availability query), `(propertyId, dateStart, dateEnd)` on blocked dates, `(propertyId, isActive, priority)` on pricing rules. Migration `20260412171311_add_booking_entities` applied to both local and hosted Supabase. Pricing rules table is empty for now — the engine consuming it lands in Phase 7; Phase 2 only needs the table to exist.
- **Task 1.8**: Live at https://staithes.vercel.app. Hosted Supabase project bootstrapped from local via [scripts/prod-bootstrap.ts](scripts/prod-bootstrap.ts) (loads `.env.production`, runs `prisma migrate deploy` + seed + storage setup, refuses to run against localhost). `postinstall: prisma generate` added so Vercel regenerates the gitignored Prisma client on every build. **Vercel + Supabase IPv6 trap hit and worth remembering**: Supabase free tier exposes the DB over IPv6 only, but Vercel build/runtime has no outbound IPv6 → `ENETUNREACH` during prerender. Fix is to use the **Transaction Pooler** hostname (`aws-0-<region>.pooler.supabase.com:6543`, username `postgres.<project-ref>`), not the direct hostname `db.<project>.supabase.co` — even with port 6543, the direct hostname only resolves IPv6. Smoke-tested: home page renders DB content, photos serve from hosted Storage via the next/image optimiser, OG/Twitter tags use the prod URL, sitemap.xml and robots.txt serve correctly. **Open follow-up**: restrict the MapTiler key to `staithes.vercel.app` (currently unrestricted in dev/prod); plug Stripe + Resend env vars in when their phases land.
- **Task 1.7**: Per-page metadata via `generateMetadata` on the home page reading from `Property` + `SiteConfiguration` (title, description, OG, Twitter card). Hero photo becomes the og:image, resolved to a full URL via `propertyPhotoUrl`. Root layout sets `metadataBase` from [lib/seo/site.ts](lib/seo/site.ts) (`NEXT_PUBLIC_SITE_URL` with localhost fallback) and a `title.template` of `"%s · Staithes"` for subpages — the home page uses `title.absolute` to opt out of the template and avoid the duplicate-brand suffix. [app/sitemap.ts](app/sitemap.ts) and [app/robots.ts](app/robots.ts) emit Next 16's typed metadata routes; sitemap pulls `lastModified` from the property's `updatedAt`; robots disallows `/api/`, `/dashboard/`, `/admin/`, `/messages/`. Verified `/sitemap.xml` and `/robots.txt` serve the expected content and the rendered `<head>` contains correct OG/Twitter tags.
- **Task 1.6**: [PropertyMap](components/property/property-map.tsx) — client component using `@maptiler/sdk` 4.0.1 with the `STREETS.PASTEL` style for a soft minimalist vibe. Approximate-location indicator is a translucent blue circle drawn as a pixel-radius circle layer (not a true geo-radius) — same privacy pattern Airbnb uses. Scroll-zoom is disabled to prevent scroll-jacking the page (Ctrl/⌘+scroll required, or use the +/- buttons). **Gotcha worth remembering**: MapTiler SDK adds a default `NavigationControl` AND `AttributionControl` automatically — passing `attributionControl: { compact: true }` configures the existing one rather than adding a second, but to add a customised navigation control you must first set `navigationControl: false` to suppress the default, otherwise you get duplicates. Renders a graceful "Map unavailable" placeholder if `NEXT_PUBLIC_MAPTILER_API_KEY` is missing. The map is reachable in the new "Where you'll be" section below the amenities.
- **Task 1.5**: Three new components — [PropertyDetails](components/property/property-details.tsx) (icon strip for guests/bedrooms/beds/bathrooms, server component), [PropertyDescription](components/property/property-description.tsx) (client; "Read more" toggle only appears when description > 320 chars), [PropertyAmenities](components/property/property-amenities.tsx) (client; preview of first 6, "Show all" dialog grouped by category). Lucide icon mapping for amenity rows lives in [lib/property/amenity-icons.ts](lib/property/amenity-icons.ts) — string→component lookup with a `Check` fallback. **React 19 / Next 16 lint gotchas hit twice and worth remembering**: (a) `react-hooks/static-components` rejects the `const Icon = item.icon; <Icon />` pattern — use `React.createElement(item.icon, props)` instead; (b) `react-hooks/preserve-manual-memoization` rejects `useMemo` because the React Compiler auto-memoises — drop manual `useMemo`/`useCallback`. Home page now has a 2-column layout on `lg+` (main content left, sticky booking placeholder right) that stacks on mobile.
- **Task 1.4**: Photo gallery component at [components/property/photo-gallery.tsx](components/property/photo-gallery.tsx) — Airbnb-style 1+4 hero grid on desktop, single hero on mobile, with a "Show all photos" button. Full-screen modal toggles between two views: a categorised grid (grouped by `PhotoCategory` enum, lazy-loaded) and a lightbox. **Lightbox is custom, not shadcn Carousel** — the shadcn `CarouselNext`/`Previous` buttons disable themselves via `canScrollNext/Prev`, and that interacted badly with `loop:true` causing the right chevron to be stuck disabled. Custom lightbox uses plain React state with modulo wrap-around for true looping, manual ←/→ keyboard handler on `window`, manual touch swipe (50px threshold), and click-outside-to-close (dark backdrop has `onClick={onClose}`; image and chevrons `stopPropagation`). Escape closes via base-ui Dialog itself. shadcn dialog component added; carousel was added but is now only used for any future non-lightbox carousels (still in `components/ui/`). **Browser-verified** before committing.
- **Task 1.3**: Created `property-photos` and `site-assets` buckets (public read) via [scripts/setup-storage.ts](scripts/setup-storage.ts) using `@supabase/supabase-js` 2.103.0 with the service role key. Script is idempotent and reads from `dev_photos/` (gitignored). Photos uploaded with predefined metadata (category, alt text, sort order) and `PropertyPhoto` rows upserted via Prisma. URL helpers in [lib/storage/photos.ts](lib/storage/photos.ts) — `propertyPhotoUrl` returns the public URL, `propertyPhotoTransformUrl`/`propertyThumbnailUrl` use Supabase's render API. **Caveat:** the transform/render API is a Supabase Pro plan feature in hosted Supabase. Enabled locally via `[storage.image_transformation] enabled = true` in `supabase/config.toml` so the helpers can be exercised in dev. On the free tier in production, fall back to next/image for optimisation. `next.config.ts` has `remotePatterns` for both local (`127.0.0.1:54421`) and hosted (`*.supabase.co`) Supabase Storage. `PropertyPhoto.url` stores the path-only value (e.g. `drone_shot.jpeg`), not the full URL — keeps the DB portable across environments. The home page now renders the highest-priority photo via next/image as the verification test.
- **Task 1.2**: Local dev uses Supabase CLI (`supabase start`) instead of a hosted instance — hosted Supabase will be wired up at deploy time. Local stack runs on bumped ports `544xx` (api 54421, db 54422, studio 54423, mailpit 54424, analytics 54427) to avoid conflict with another local Supabase project (`savi-local`) on this machine — set in `supabase/config.toml`. Prisma 7.7.0 uses the new `prisma-client` generator (output → `lib/generated/prisma`, gitignored) plus the `@prisma/adapter-pg` driver adapter — both required in v7. Connection URL is no longer in `schema.prisma`; it lives in `prisma.config.ts`, which loads `.env.local` via `dotenv` for migrations (Next.js handles runtime env loading). Singleton client at [lib/db/prisma.ts](lib/db/prisma.ts). Phase 1 schema covers `Property`, `PropertyPhoto`, `Amenity`, `PropertyAmenity`, `SiteConfiguration` only — booking/payment/messaging/review entities will be added in their respective phases. Seed populates one placeholder property (`The Staithes Cottage`) plus 15 reference amenities with 10 linked. Verified end-to-end at [`/api/property`](app/api/property/route.ts).

